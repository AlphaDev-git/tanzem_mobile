import 'package:flutter/material.dart';

//all App Colors

abstract class AppColors {
  static const primary = Color(0xFF18C5B6); // قريب من لون التصميم
  static const secondary = Color(0xFF00A8A8);
  static const textDark = Color(0xFF1E1E1E);
  static const textLight = Color(0xFF6E6E6E);
}
